{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key for AI-powered security briefings and live chat. Optional — app has graceful fallbacks without it.",
      "requiredAtBoot": false
    }
  ]
}
